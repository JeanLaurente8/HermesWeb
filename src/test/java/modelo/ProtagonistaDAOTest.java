    /*public void testCrearProtagonistaEnMemoria() {
package modelo;

import java.time.LocalDate;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

public class ProtagonistaDAOTest {

    @Test
    /*public void testCrearProtagonistaEnMemoria() {

        /*Protagonista protagonista = new Protagonista(
                "Juan", // nombre
                "Pérez", // apellido
                "Peruana", // nacionalidad
                LocalDate.of(1990, 5, 12),// fechaNacimiento
                "Actor de cine reconocido por su versatilidad.", // biografia
                "Soltero", // estado civil
                "Oscar 2020", // premios
                1.75, // altura
                75.0 // peso
        );

        // Validaciones correctas
        Assertions.assertEquals("Juan", protagonista.getNombre());
        Assertions.assertEquals("Pérez", protagonista.getApellido());
        Assertions.assertEquals("Peruana", protagonista.getNacionalidad());
        Assertions.assertEquals(LocalDate.of(1990, 5, 12), protagonista.getFechaNacimiento());
        Assertions.assertEquals("Soltero", protagonista.getEstadoCivil());
        Assertions.assertEquals(Double.valueOf(1.75), protagonista.getAltura());
        Assertions.assertEquals(Double.valueOf(75.0), protagonista.getPeso());
        Assertions.assertNotNull(protagonista);
    }
}*/
